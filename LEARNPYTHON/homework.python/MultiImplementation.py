# Find the percentage of 3 different subjects and it fulfil the below requirements:
# take an input from user
# Use the argument and return type function
# Function should be called in different file
# Use the class concept as well

class students_percentage():
    def percentage(self,math,english,science): #Argument
          total = math + english + science
          per = (total/3)
          return per #Return

